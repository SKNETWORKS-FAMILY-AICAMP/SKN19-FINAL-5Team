# DDoksori 검색 API - DB 연결 및 테스트 안내

**작성일**: 2026-01-26
**대상**: 팀원 전체
**목적**: AWS RDS 연결 및 검색 API 테스트 방법 안내

> 💡 **AWS RDS 상세 정보**: `03_03_DB_n_API결과.md` 파일을 참고하세요.

---

## 📋 목차

1. [사전 준비사항](#1-사전-준비사항)
2. [AWS RDS 연결 설정](#2-aws-rds-연결-설정)
3. [검색 API 서버 실행](#3-검색-api-서버-실행)
4. [테스트 방법](#4-테스트-방법)
5. [문제 해결](#5-문제-해결)

---

## 1. 사전 준비사항

### 1.1 필수 소프트웨어 설치

#### Python 패키지 설치
```bash
pip install fastapi uvicorn psycopg2-binary openai python-dotenv requests
```

또는 requirements.txt 사용:
```bash
cd C:\Users\Playdata\Desktop\project\05_hub_5th_project\data_n_db
pip install -r requirements.txt
```

#### PostgreSQL 클라이언트 설치 (선택사항)
- **DBeaver** (추천): https://dbeaver.io/download/
- **pgAdmin**: https://www.pgadmin.org/download/
- **DataGrip** (JetBrains): https://www.jetbrains.com/datagrip/

### 1.2 필요한 정보

팀장/DB 관리자에게 다음 정보를 받으세요:

- ✅ AWS RDS 엔드포인트: `dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com`
- ✅ 데이터베이스 이름: `ddoksori`
- ✅ 사용자명: `postgres`
- ✅ 비밀번호: (별도 공유)
- ✅ 포트 번호: `5432`
- ✅ OpenAI API Key: (별도 공유)

---

## 2. AWS RDS 연결 설정

### 2.1 AWS RDS 인스턴스 정보

**구축 완료된 AWS RDS 정보** (2026-01-26 기준):

| 항목 | 값 |
|------|-----|
| **엔드포인트** | dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com |
| **포트** | 5432 |
| **데이터베이스** | ddoksori |
| **사용자** | postgres |
| **리전** | us-east-1 (미국 버지니아 북부) |
| **DB 엔진** | PostgreSQL 17.2-R3 |
| **pgvector 버전** | 0.8.1 |
| **인스턴스 클래스** | db.r7g.xlarge (4 vCPU, 32GB RAM) |
| **총 데이터** | 40,285 청크 (law_guide 6,077 + case 34,208) |

### 2.2 .env 파일 생성

프로젝트 루트에 `.env` 파일을 생성합니다:

**경로**: `C:\Users\Playdata\Desktop\project\05_hub_5th_project\data_n_db\.env`

```bash
# OpenAI API Key (팀장에게 문의)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# AWS RDS PostgreSQL 연결 정보
DB_HOST=dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=ddoksori
DB_USER=postgres
DB_PASSWORD=your-password-here

# 검색 API URL (로컬 테스트용)
SEARCH_API_URL=http://localhost:8000
```

**⚠️ 주의사항:**
- `.env` 파일은 절대 GitHub에 커밋하지 마세요!
- 이미 `.gitignore`에 추가되어 있습니다.
- `DB_PASSWORD`와 `OPENAI_API_KEY`는 팀장에게 별도로 받으세요.

### 2.3 DB 연결 테스트

#### 방법 1: Python 스크립트로 테스트

`test_db_connection.py` 파일 생성:

```python
import psycopg2
from dotenv import load_dotenv
import os
from pathlib import Path

# .env 파일 로드
env_path = Path(__file__).parent / '.env'
load_dotenv(dotenv_path=env_path)

DB_CONFIG = {
    "host": os.getenv("DB_HOST"),
    "port": int(os.getenv("DB_PORT", "5432")),
    "database": os.getenv("DB_NAME"),
    "user": os.getenv("DB_USER"),
    "password": os.getenv("DB_PASSWORD")
}

try:
    print("🔌 AWS RDS 연결 테스트 중...")
    print(f"   Host: {DB_CONFIG['host']}")
    print(f"   Database: {DB_CONFIG['database']}")
    print(f"   User: {DB_CONFIG['user']}")

    conn = psycopg2.connect(**DB_CONFIG, connect_timeout=10)
    cursor = conn.cursor()

    # PostgreSQL 버전 확인
    cursor.execute("SELECT version();")
    version = cursor.fetchone()
    print(f"\n✅ 연결 성공!")
    print(f"   PostgreSQL Version: {version[0][:80]}...")

    # pgvector 확인
    cursor.execute("""
        SELECT extname, extversion
        FROM pg_extension
        WHERE extname = 'vector'
    """)
    pgvector = cursor.fetchone()
    if pgvector:
        print(f"   pgvector Version: {pgvector[1]}")

    # vector_chunks 테이블 확인
    cursor.execute("SELECT COUNT(*) FROM vector_chunks")
    chunk_count = cursor.fetchone()[0]
    print(f"   총 청크 수: {chunk_count:,}개")

    # 데이터셋별 통계
    cursor.execute("""
        SELECT dataset_type, COUNT(*)
        FROM vector_chunks
        GROUP BY dataset_type
    """)
    for row in cursor.fetchall():
        print(f"   - {row[0]}: {row[1]:,}개")

    cursor.close()
    conn.close()
    print("\n🎉 DB 연결 테스트 완료!")

except Exception as e:
    print(f"\n❌ 연결 실패: {e}")
    print("\n📌 체크리스트:")
    print("   1. .env 파일의 DB_PASSWORD가 정확한가요?")
    print("   2. 인터넷 연결이 정상인가요?")
    print("   3. 팀장에게 연결 정보를 확인하셨나요?")
```

**실행:**
```bash
python test_db_connection.py
```

**성공 시 출력 예시:**
```
🔌 AWS RDS 연결 테스트 중...
   Host: dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com
   Database: ddoksori
   User: postgres

✅ 연결 성공!
   PostgreSQL Version: PostgreSQL 17.2 on aarch64-unknown-linux-gnu...
   pgvector Version: 0.8.1
   총 청크 수: 40,285개
   - law_guide: 6,077개
   - case: 34,208개

🎉 DB 연결 테스트 완료!
```

#### 방법 2: DBeaver로 연결

1. DBeaver 실행
2. "새 데이터베이스 연결" 클릭
3. "PostgreSQL" 선택
4. 연결 정보 입력:
   - **Host**: `dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com`
   - **Port**: `5432`
   - **Database**: `ddoksori`
   - **Username**: `postgres`
   - **Password**: (팀장에게 문의)
5. "Test Connection" 클릭
6. 성공하면 "Finish" 클릭

**연결 후 확인:**
```sql
-- 1. pgvector 설치 확인
SELECT extname, extversion FROM pg_extension WHERE extname = 'vector';

-- 2. 총 청크 수 확인
SELECT COUNT(*) FROM vector_chunks;

-- 3. 데이터셋별 통계
SELECT dataset_type, COUNT(*)
FROM vector_chunks
GROUP BY dataset_type;

-- 4. 카테고리별 통계 (case 데이터)
SELECT category, COUNT(*)
FROM vector_chunks
WHERE dataset_type = 'case'
GROUP BY category;
```

---

## 3. 검색 API 서버 실행

### 3.1 API 서버 실행

**터미널 1** (API 서버용)

```bash
cd C:\Users\Playdata\Desktop\project\05_hub_5th_project\data_n_db\DB
python 03_01_search_api.py
```

**성공 시 출력:**
```
================================================================================
DDoksori Vector Search API
================================================================================
Database: dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com:5432/ddoksori
Starting server at http://localhost:8000
API Docs: http://localhost:8000/docs
================================================================================
[OK] Database connected successfully (40,285 chunks loaded)
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
```

### 3.2 API 문서 확인

브라우저에서 접속:
```
http://localhost:8000/docs
```

**Swagger UI**가 표시되며 다음 엔드포인트들을 확인할 수 있습니다:
- `GET /` - API 정보
- `GET /health` - 헬스 체크 (총 청크 수 확인)
- `GET /stats` - 데이터베이스 통계
- `POST /search` - 검색 (메인)

---

## 4. 테스트 방법

### 4.1 방법 1: 기본 테스트 스크립트 사용 (가장 쉬움)

**터미널 2** (API 서버는 계속 실행 중)

```bash
cd C:\Users\Playdata\Desktop\project\05_hub_5th_project\data_n_db\DB
python 03_02_test_search_api.py
```

**실행 결과:**
```
================================================================================
1. Health Check
================================================================================
Status Code: 200
{
  "status": "healthy",
  "database": "connected",
  "total_chunks": 40285
}

================================================================================
2. Database Statistics
================================================================================
총 6개 그룹:
  - case       | 조정    | case            | 20992개 청크
  - case       | 상담    | case            | 11342개 청크
  - case       | 해결    | case            |  1874개 청크
  - law_guide  | N/A    | law_ED          |  4111개 청크
  - law_guide  | N/A    | guide           |  1966개 청크

================================================================================
3. Hybrid Search (BM25 + Vector + RRF)
================================================================================
Query: 환불 거부 시 소비자 권리는?
Total Results: 5
Search Time: 2431.41ms

[1] Score: 0.0164 | law_guide | law_ED_조_전체_1
    Text: 소비자기본법 제16조 소비자의 기본적 권리...
    Law: 소비자기본법

...
```

### 4.2 방법 2: Swagger UI로 테스트 (추천)

1. 브라우저에서 `http://localhost:8000/docs` 접속
2. `/search` 엔드포인트 클릭
3. "Try it out" 버튼 클릭
4. Request body에 다음 입력:

```json
{
  "query": "노트북 디자인이 별로라서 환불받고 싶은데, 유사한 사례가 있어?",
  "search_type": "hybrid",
  "top_k": 5
}
```

5. "Execute" 버튼 클릭
6. 아래에 검색 결과 확인

**검색 옵션:**

| 파라미터 | 타입 | 설명 | 기본값 |
|---------|------|------|--------|
| `query` | string | 검색 질문 (필수) | - |
| `search_type` | string | `hybrid`, `vector`, `bm25` 중 선택 | `hybrid` |
| `top_k` | integer | 반환할 결과 개수 (1~100) | 10 |
| `dataset_filter` | string | `law_guide` (법령만) 또는 `case` (사례만) | null |
| `category_filter` | string | `상담`, `해결`, `조정` 중 선택 | null |
| `year_filter` | integer | 연도 필터 (2010~2024) | null |

### 4.3 방법 3: Python 스크립트로 커스텀 테스트

`my_test.py` 파일 생성:

```python
import requests
import json

API_URL = "http://localhost:8000"

def search_case(question, category=None, dataset=None):
    """사용자 질문으로 검색"""

    payload = {
        "query": question,
        "search_type": "hybrid",  # hybrid, vector, bm25 중 선택
        "top_k": 5
    }

    if category:
        payload["category_filter"] = category
    if dataset:
        payload["dataset_filter"] = dataset

    print(f"\n{'='*80}")
    print(f"질문: {question}")
    if category:
        print(f"카테고리 필터: {category}")
    if dataset:
        print(f"데이터셋 필터: {dataset}")
    print('='*80)

    response = requests.post(f"{API_URL}/search", json=payload)

    if response.status_code != 200:
        print(f"❌ 오류: {response.status_code}")
        print(response.text)
        return

    result = response.json()

    print(f"\n검색 결과: {result['total_results']}건")
    print(f"검색 시간: {result['search_time_ms']}ms\n")

    for i, item in enumerate(result['results'], 1):
        print(f"\n[{i}] 점수: {item['score']:.4f}")
        print(f"    데이터 타입: {item['dataset_type']}")

        if item.get('category'):
            print(f"    카테고리: {item['category']}")

        if item.get('law_name'):
            print(f"    법령명: {item['law_name']}")

        if item.get('source_url'):
            print(f"    출처 URL: {item['source_url'][:80]}...")

        # 내용 미리보기
        text_preview = item['text'][:200].replace('\n', ' ')
        print(f"\n    내용: {text_preview}...")
        print(f"    {'-'*76}")

# 테스트 질문들
if __name__ == "__main__":
    # 예시 1: 환불 관련 질문
    search_case("노트북 디자인이 별로라서 환불받고 싶은데, 유사한 사례가 있어?")

    input("\n다음 검색을 보려면 Enter를 누르세요...")

    # 예시 2: 배송 지연 문제 (해결사례만)
    search_case("배송이 한 달째 안 오는데 어떻게 해야 하나요?", category="해결")

    input("\n다음 검색을 보려면 Enter를 누르세요...")

    # 예시 3: 법령 검색
    search_case("소비자기본법 제16조 내용이 궁금해요", dataset="law_guide")

    print("\n테스트 완료!")
```

**실행:**
```bash
python my_test.py
```

### 4.4 테스트용 질문 예시

팀원들이 테스트해볼 수 있는 다양한 질문:

#### 환불/청약철회 관련
```
- "노트북 디자인이 별로라서 환불받고 싶은데, 유사한 사례가 있어?"
- "단순 변심으로 환불받을 수 있나요?"
- "인터넷 쇼핑몰에서 옷을 샀는데 사진과 달라요. 환불 가능한가요?"
- "청약철회 기간은 언제까지인가요?"
```

#### 배송 관련
```
- "배송이 한 달째 안 오는데 어떻게 해야 하나요?"
- "배송 중 파손된 물건은 누가 책임지나요?"
- "배송 지연으로 피해를 봤어요. 보상받을 수 있나요?"
```

#### 제품 하자/AS 관련
```
- "제품 하자가 발견됐는데 AS 거부당했어요"
- "중고폰을 샀는데 고장났어요. 환불 가능한가요?"
- "가전제품 수리비가 너무 비싼데 어떻게 하죠?"
```

#### 계약 관련
```
- "헬스장 계약 해지하고 싶은데 위약금이 너무 비싸요"
- "렌탈 중도 해지 시 위약금이 얼마나 되나요?"
- "이사 때문에 인터넷 계약 해지하고 싶어요"
```

#### 법령 검색
```
- "소비자기본법 제16조 내용이 궁금해요"
- "전자상거래법 청약철회 조항 찾아줘"
- "할부거래에 관한 법률 위약금 규정"
```

---

## 5. 문제 해결

### 5.1 "Database connection failed" 오류

**증상:**
```
[ERROR] Database connection failed after 3 attempts: could not connect to server
```

**해결 방법:**

1. **.env 파일 확인**
   ```bash
   # DB_HOST가 정확한지 확인
   DB_HOST=dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com
   DB_PORT=5432
   DB_NAME=ddoksori
   DB_USER=postgres
   DB_PASSWORD=올바른비밀번호
   ```

2. **인터넷 연결 확인**
   ```bash
   # Windows
   ping dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com

   # 연결 테스트
   Test-NetConnection -ComputerName dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com -Port 5432
   ```

3. **팀장에게 확인**
   - AWS RDS 인스턴스가 실행 중인지
   - 보안 그룹에 본인 IP가 허용되어 있는지

### 5.2 "Embedding generation failed" 오류

**증상:**
```
[ERROR] Embedding generation failed: Error code: 401 - {'error': {'message': 'Incorrect API key'}}
```

**해결 방법:**

1. **OpenAI API Key 확인**
   ```bash
   # .env 파일에서 OPENAI_API_KEY 확인
   # sk-proj-로 시작해야 함
   OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxx
   ```

2. **API Key 유효성 확인**
   - OpenAI 대시보드 (https://platform.openai.com/api-keys) 접속
   - API Key가 활성화되어 있는지 확인

3. **팀장에게 API Key 요청**

### 5.3 "Cannot connect to API server" 오류

**증상:**
```bash
[ERROR] Cannot connect to API server
Make sure the API server is running: python 03_01_search_api.py
```

**해결 방법:**

1. **API 서버 실행 확인**
   ```bash
   # 새 터미널에서
   cd C:\Users\Playdata\Desktop\project\05_hub_5th_project\data_n_db\DB
   python 03_01_search_api.py
   ```

2. **포트 8000 사용 확인**
   ```bash
   # Windows
   netstat -ano | findstr :8000

   # 다른 프로그램이 사용 중이면 포트 변경
   ```

### 5.4 검색 결과가 없거나 이상한 경우

**확인 사항:**

1. **데이터베이스 확인**
   ```python
   # test_db_connection.py 실행하여 청크 수 확인
   python test_db_connection.py
   # 출력: 총 청크 수: 40,285개
   ```

2. **검색 타입 변경**
   - `hybrid` 검색 권장 (BM25 + 벡터 통합)
   - 키워드 검색만 필요하면 `bm25`
   - 의미 기반 검색만 필요하면 `vector`

3. **필터 사용**
   ```json
   {
     "query": "환불 사례",
     "dataset_filter": "case",  // 사례만 검색
     "category_filter": "해결"   // 해결사례만
   }
   ```

### 5.5 느린 검색 속도

**현재 성능** (AWS RDS db.r7g.xlarge):
- 첫 검색: 약 2.4초 (임베딩 생성 포함)
- 이후 검색: 약 2.4초 (네트워크 지연)

**개선 방법:**

1. **top_k 값 줄이기**
   ```json
   {
     "query": "...",
     "top_k": 3  // 10 대신 3으로
   }
   ```

2. **필터 사용**으로 검색 범위 축소

3. **검색 타입 변경**: `hybrid` → `vector` 또는 `bm25`

---

## 6. 체크리스트

### 연결 전 체크리스트

- [ ] Python 3.8 이상 설치됨
- [ ] 필수 패키지 설치 완료 (`pip install -r requirements.txt`)
- [ ] `.env` 파일 생성 및 연결 정보 입력 완료
  - [ ] `DB_HOST=dsr-postgres.cyhiie0gambz.us-east-1.rds.amazonaws.com`
  - [ ] `DB_NAME=ddoksori`
  - [ ] `DB_USER=postgres`
  - [ ] `DB_PASSWORD=팀장에게문의`
  - [ ] `OPENAI_API_KEY=팀장에게문의`
- [ ] DB 연결 테스트 성공 (`python test_db_connection.py`)
  - [ ] 총 청크 40,285개 확인

### API 테스트 체크리스트

- [ ] API 서버 실행 성공 (`python 03_01_search_api.py`)
- [ ] `/health` 엔드포인트에서 `total_chunks: 40285` 확인
- [ ] `/stats` 엔드포인트에서 데이터 통계 확인
- [ ] 기본 테스트 스크립트 실행 성공 (`python 03_02_test_search_api.py`)
- [ ] Swagger UI 접속 성공 (`http://localhost:8000/docs`)
- [ ] 샘플 질문으로 검색 테스트 성공

---

## 7. 추가 정보

### 데이터베이스 통계 (2026-01-26 기준)

**총 데이터**: 40,285 청크

**데이터셋별**:
- law_guide: 6,077건 (법령 + 가이드)
- case: 34,208건 (상담 + 해결 + 조정 사례)

**카테고리별** (case):
- 조정: 20,992건 (61.3%)
- 상담: 11,342건 (33.2%)
- 해결: 1,874건 (5.5%)

**연도별** (case): 2010~2024년 (720건에 연도 정보 있음)

### API 엔드포인트 요약

| 엔드포인트 | 메소드 | 설명 | 예시 |
|-----------|--------|------|------|
| `/` | GET | API 정보 | `curl http://localhost:8000/` |
| `/health` | GET | 헬스 체크 | `curl http://localhost:8000/health` |
| `/stats` | GET | DB 통계 | `curl http://localhost:8000/stats` |
| `/search` | POST | 검색 | Swagger UI 또는 Python 스크립트 |

### 검색 타입 비교

| 검색 타입 | 장점 | 단점 | 사용 시기 |
|---------|------|------|---------|
| `hybrid` | 키워드+의미 통합, 가장 정확 | 약간 느림 (2.4초) | 일반적인 질문 (권장) |
| `vector` | 의미 기반, 유연함 | 정확한 키워드 매칭 약함 | 개념/의미 기반 검색 |
| `bm25` | 빠름, 키워드 정확 | 의미 이해 안 됨 | 특정 키워드 찾기 |

---

## 8. 문의 및 지원

### 필요한 정보 요청

팀장에게 다음 정보를 요청하세요:
1. AWS RDS 비밀번호 (`DB_PASSWORD`)
2. OpenAI API Key (`OPENAI_API_KEY`)

### 유용한 링크

- **Swagger UI**: http://localhost:8000/docs (API 서버 실행 후)
- **AWS RDS 상세 정보**: `03_03_DB_n_API결과.md` 파일 참고
- **OpenAI API Docs**: https://platform.openai.com/docs
- **FastAPI Docs**: https://fastapi.tiangolo.com/

---

## 9. API 서버 접속 문제 해결

### 9.1 문제 상황

`python 03_01_search_api.py` 실행 후 `http://localhost:8000/docs` 접속이 안 되는 경우

### 9.2 서버 실행 확인

#### 1단계: 터미널 출력 확인

서버가 정상적으로 실행되면 다음과 같은 메시지가 출력됩니다:

```
INFO:     Started server process [12345]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://127.0.0.1:8000 (Press CTRL+C to quit)
```

#### 2단계: 포트 사용 확인

**Windows:**
```bash
netstat -ano | findstr :8000
```

**Mac/Linux:**
```bash
lsof -i :8000
```

포트가 사용 중이면 프로세스 번호(PID)가 표시됩니다.

### 9.3 일반적인 오류 및 해결 방법

#### 오류 1: DB 연결 실패

**증상:**
```
sqlalchemy.exc.OperationalError: could not connect to server
```

**원인:**
- `.env` 파일 누락 또는 잘못된 비밀번호
- AWS RDS 보안 그룹 설정 문제
- 네트워크 연결 문제

**해결:**
1. `.env` 파일이 `DB/` 폴더에 있는지 확인
2. `DB_PASSWORD` 값이 정확한지 확인 (팀장에게 문의)
3. 인터넷 연결 확인
4. VPN 사용 중이면 해제 후 재시도

#### 오류 2: 포트가 이미 사용 중

**증상:**
```
ERROR: [Errno 10048] error while attempting to bind on address ('0.0.0.0', 8000)
```

**원인:**
- 다른 프로그램이 8000번 포트 사용 중
- 이전 실행이 제대로 종료되지 않음

**해결:**

**Windows:**
```bash
# 포트 사용 프로세스 찾기
netstat -ano | findstr :8000

# 프로세스 종료 (PID는 위에서 확인한 번호)
taskkill /PID [프로세스번호] /F
```

**Mac/Linux:**
```bash
# 포트 사용 프로세스 찾기
lsof -i :8000

# 프로세스 종료
kill -9 [PID]
```

또는 다른 포트로 실행:
```bash
uvicorn 03_01_search_api:app --host 0.0.0.0 --port 8001
# 그 다음 http://localhost:8001/docs 접속
```

#### 오류 3: 필요한 패키지 누락

**증상:**
```
ModuleNotFoundError: No module named 'fastapi'
```

**해결:**
```bash
pip install fastapi uvicorn sqlalchemy psycopg2-binary openai python-dotenv
```

#### 오류 4: 방화벽 차단

**증상:**
- 서버는 실행되지만 브라우저에서 접속 안 됨

**해결:**

**Windows:**
1. Windows Defender 방화벽 → 고급 설정
2. 인바운드 규칙 → 새 규칙
3. 포트 → TCP → 특정 로컬 포트 → 8000
4. 연결 허용 → 완료

**Mac:**
```bash
# 방화벽 상태 확인
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --getglobalstate

# 파이썬 허용
sudo /usr/libexec/ApplicationFirewall/socketfilterfw --add /usr/bin/python3
```

### 9.4 단계별 진단 체크리스트

다음 순서대로 확인하세요:

- [ ] **1. 서버 실행 확인**
  ```bash
  python 03_01_search_api.py
  ```
  "Uvicorn running on..." 메시지가 나오는지 확인

- [ ] **2. 다른 터미널에서 포트 확인**
  ```bash
  # Windows
  netstat -ano | findstr :8000

  # Mac/Linux
  lsof -i :8000
  ```

- [ ] **3. 브라우저 접속**
  - http://localhost:8000/docs
  - http://127.0.0.1:8000/docs (위가 안 되면 이걸 시도)

- [ ] **4. curl로 테스트** (브라우저가 안 되면)
  ```bash
  curl http://localhost:8000/docs
  ```

- [ ] **5. DB 연결 테스트**
  ```python
  # test_db_connection.py 실행
  python test_db_connection.py
  ```

- [ ] **6. .env 파일 확인**
  ```bash
  # DB/ 폴더에 .env 파일이 있는지
  dir .env  # Windows
  ls -la .env  # Mac/Linux
  ```

### 9.5 빠른 해결 방법 (자주 발생하는 문제 Top 3)

#### 1. 가장 흔한 원인: DB 비밀번호 오류

```bash
# .env 파일 확인
cat .env  # Mac/Linux
type .env  # Windows

# 비밀번호가 정확한지 팀장에게 확인
```

#### 2. 두 번째 흔한 원인: 포트 충돌

```bash
# 다른 포트로 실행
uvicorn 03_01_search_api:app --port 8001

# 브라우저에서 접속
http://localhost:8001/docs
```

#### 3. 세 번째 흔한 원인: 잘못된 URL

❌ **틀린 URL:**
- `http://localhost:8000` (Swagger UI 경로 누락)
- `https://localhost:8000/docs` (https 사용 불가)

✅ **올바른 URL:**
- `http://localhost:8000/docs`
- `http://127.0.0.1:8000/docs`

### 9.6 여전히 안 될 때

다음 정보를 팀장에게 공유하세요:

1. **오류 메시지 전체 내용** (터미널 스크린샷)
2. **환경 정보**:
   ```bash
   python --version
   pip list | grep -E "fastapi|uvicorn|sqlalchemy"
   ```
3. **포트 상태**:
   ```bash
   netstat -ano | findstr :8000  # Windows
   lsof -i :8000  # Mac/Linux
   ```

---

**작성일**: 2026-01-26
**최종 수정**: 2026-01-26
**버전**: 1.2.0 (API 서버 접속 문제 해결 추가)
