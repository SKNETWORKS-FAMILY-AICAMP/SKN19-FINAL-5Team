# PostgreSQL + pgvector 데이터베이스 구축 가이드 (Docker/로컬)

**작성일**: 2026-01-23
**목적**: Docker를 활용한 크로스 플랫폼 DB 환경 구축 및 팀원 간 공유

**⚠️ 중요**: 팀 프로젝트의 경우 **`01_02_AWS_RDS구축방법.md`를 먼저 확인**하세요!
AWS RDS가 팀 간 데이터 공유 및 협업에 훨씬 유리합니다.

**이 문서는 로컬 개발 환경 구축을 위한 가이드입니다.**

---

## 📋 목차

1. [구축 방법 선택](#구축-방법-선택)
2. [방법 A: Docker 환경 구축 (권장)](#방법-a-docker-환경-구축-권장)
3. [방법 B: 로컬 설치 (Windows)](#방법-b-로컬-설치-windows)
4. [데이터베이스 세팅](#데이터베이스-세팅)
5. [데이터 삽입](#데이터-삽입)
6. [문제 해결](#문제-해결)
7. [마이그레이션 체크리스트](#마이그레이션-체크리스트)

---

## 🎯 구축 방법 선택

### 방법 비교

| 구분 | AWS RDS (권장 ⭐) | Docker (로컬) | 로컬 설치 |
|-----|-----------------|--------------|---------|
| **팀원 공유** | ✅ URL만 공유 | ⚠️ 덤프/복원 필요 | ❌ 개별 설치 필요 |
| **데이터 동기화** | ✅ 실시간 | ❌ 수동 | ❌ 수동 |
| **크로스 플랫폼** | ✅ 모든 OS | ✅ Windows/Mac/Linux | ❌ OS별 상이 |
| **비용** | 💰 월 $17-20 (1년 무료) | 무료 | 무료 |
| **설치 난이도** | ⭐⭐ (AWS CLI) | ⭐⭐ (Docker) | ⭐⭐⭐⭐ (복잡) |
| **성능** | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **팀 프로젝트** | ✅ 최적 | ⚠️ 가능 | ❌ 비권장 |

**결론**:
- **팀 프로젝트**: `01_02_AWS_RDS구축방법.md` 참고 (강력 권장)
- **로컬 개발/테스트**: Docker 사용 (이 문서)
- **빠른 프로토타이핑**: Docker 사용 (이 문서)

---

## 🐳 방법 A: Docker 환경 구축 (권장)

### 사전 요구사항

#### Windows 사용자
- **Windows 10/11** (64-bit)
- **WSL 2** (Windows Subsystem for Linux 2)
- **Docker Desktop for Windows**
- 최소 8GB RAM (16GB 권장)
- 10GB 이상 여유 디스크 공간

#### Mac 사용자
- **macOS 11 Big Sur** 이상
- **Docker Desktop for Mac**
  - Intel Mac: amd64 버전
  - Apple Silicon (M1/M2/M3): arm64 버전
- 최소 8GB RAM (16GB 권장)
- 10GB 이상 여유 디스크 공간

---

### 1단계: Docker 설치

#### Windows에서 Docker 설치

1. **WSL 2 설치 및 활성화**:
   ```powershell
   # PowerShell을 관리자 권한으로 실행
   wsl --install

   # 재부팅 후 WSL 2를 기본값으로 설정
   wsl --set-default-version 2
   ```

2. **Docker Desktop 다운로드 및 설치**:
   - URL: https://www.docker.com/products/docker-desktop/
   - "Download for Windows" 클릭
   - 설치 파일 실행 (`Docker Desktop Installer.exe`)
   - 설치 옵션:
     - [✓] Use WSL 2 instead of Hyper-V (권장)
     - [✓] Add shortcut to desktop

3. **Docker Desktop 실행 및 확인**:
   - 시작 메뉴에서 "Docker Desktop" 실행
   - 하단 상태 바가 녹색(Running)으로 변경될 때까지 대기

4. **PowerShell/CMD에서 확인**:
   ```bash
   docker --version
   docker-compose --version
   ```

   **예상 출력**:
   ```
   Docker version 25.0.0, build xxx
   Docker Compose version v2.24.0
   ```

#### Mac에서 Docker 설치

1. **Docker Desktop 다운로드**:
   - URL: https://www.docker.com/products/docker-desktop/
   - Mac 칩에 따라 선택:
     - **Intel Mac**: "Mac with Intel chip" 다운로드
     - **Apple Silicon (M1/M2/M3)**: "Mac with Apple chip" 다운로드

2. **설치**:
   - `.dmg` 파일 다운로드 후 실행
   - Docker.app을 Applications 폴더로 드래그
   - Applications에서 Docker 실행
   - 권한 요청 시 승인

3. **터미널에서 확인**:
   ```bash
   docker --version
   docker-compose --version
   ```

---

### 2단계: Docker Compose 파일 생성

#### 2.1 프로젝트 디렉토리 구조

```
data_n_db/
├── DB/
│   ├── docker-compose.yml          # Docker 설정 파일
│   ├── .env                         # 환경 변수 (비밀번호 등)
│   ├── 01_00_unified_schema.sql    # 스키마 파일
│   ├── init/                        # 초기화 스크립트 (자동 실행)
│   │   └── 01_init_db.sql
│   └── data/                        # PostgreSQL 데이터 (볼륨)
└── (프로젝트 나머지 파일들)
```

#### 2.2 docker-compose.yml 작성

프로젝트 디렉토리에 `docker-compose.yml` 파일을 생성합니다:

```yaml
version: '3.8'

services:
  postgres:
    image: pgvector/pgvector:pg17
    container_name: ddoksori_db
    restart: unless-stopped

    # 환경 변수
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-ddoksori}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "--encoding=UTF-8 --locale=C"
      TZ: Asia/Seoul

    # 포트 매핑
    ports:
      - "${POSTGRES_PORT:-5432}:5432"

    # 볼륨 마운트
    volumes:
      # PostgreSQL 데이터 영구 저장
      - postgres_data:/var/lib/postgresql/data

      # 초기화 스크립트 (최초 실행 시 자동 실행)
      - ./01_00_unified_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro

      # 추가 설정 파일 (선택사항)
      # - ./postgresql.conf:/etc/postgresql/postgresql.conf:ro

    # 헬스 체크
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-ddoksori}"]
      interval: 10s
      timeout: 5s
      retries: 5

    # 리소스 제한 (선택사항)
    deploy:
      resources:
        limits:
          cpus: '2'
          memory: 4G
        reservations:
          memory: 2G

  # CloudBeaver (선택사항, 웹 기반 DB 관리 도구)
  # DBeaver 데스크톱 앱을 사용하는 경우 이 서비스는 불필요합니다
  # 웹 기반 DB 접속을 원하면 아래 주석을 해제하세요
  # cloudbeaver:
  #   image: dbeaver/cloudbeaver:latest
  #   container_name: ddoksori_cloudbeaver
  #   restart: unless-stopped
  #
  #   ports:
  #     - "${CLOUDBEAVER_PORT:-8978}:8978"
  #
  #   volumes:
  #     - cloudbeaver_data:/opt/cloudbeaver/workspace
  #
  #   depends_on:
  #     postgres:
  #       condition: service_healthy

# 볼륨 정의
volumes:
  postgres_data:
    name: ddoksori_postgres_data
  # cloudbeaver_data:
  #   name: ddoksori_cloudbeaver_data
```

#### 2.3 .env 파일 작성

**중요**: `.env` 파일에 비밀번호 등 민감한 정보를 저장합니다.

```bash
# DB/.env 파일 생성 (프로젝트 루트에서)
cd data_n_db/DB
notepad .env  # Windows
# 또는
nano .env     # Mac/Linux
```

**내용**:
```env
# PostgreSQL 설정
POSTGRES_DB=ddoksori
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YourSecurePassword123!
POSTGRES_PORT=5432

# CloudBeaver 설정 (선택사항, 웹 기반 DB 도구)
# DBeaver 데스크톱 앱을 사용하면 불필요
# CLOUDBEAVER_PORT=8978

# OpenAI API (나중에 사용)
OPENAI_API_KEY=sk-proj-...
```

**중요**: `.env` 파일은 절대 Git에 커밋하지 마세요! `.gitignore`에 추가하세요.

---

### 3단계: Docker 컨테이너 실행

#### 3.1 컨테이너 시작

```bash
# DB 디렉토리로 이동 (프로젝트 루트에서)
cd data_n_db/DB

# Docker Compose로 컨테이너 시작 (-d: 백그라운드 실행)
docker-compose up -d
```

**예상 출력**:
```
[+] Running 2/2
 ✔ Network db_default      Created
 ✔ Container ddoksori_db   Started
```

#### 3.2 컨테이너 상태 확인

```bash
# 실행 중인 컨테이너 확인
docker-compose ps
```

**예상 출력**:
```
NAME            IMAGE                    STATUS              PORTS
ddoksori_db     pgvector/pgvector:pg17   Up 10 seconds       0.0.0.0:5432->5432/tcp
```

#### 3.3 PostgreSQL 연결 테스트

**방법 1: Docker exec로 psql 접속**
```bash
# PostgreSQL 컨테이너 내부에서 psql 실행
docker exec -it ddoksori_db psql -U postgres -d ddoksori
```

**방법 2: 로컬 psql 클라이언트 사용** (psql이 설치된 경우)
```bash
psql -h localhost -p 5432 -U postgres -d ddoksori
```

**방법 3: Python으로 연결 테스트**
```python
# test_connection.py
import os
import psycopg2
from dotenv import load_dotenv

load_dotenv()

try:
    conn = psycopg2.connect(
        host="localhost",
        port=os.getenv("POSTGRES_PORT", 5432),
        database=os.getenv("POSTGRES_DB"),
        user=os.getenv("POSTGRES_USER"),
        password=os.getenv("POSTGRES_PASSWORD")
    )

    cursor = conn.cursor()
    cursor.execute("SELECT version();")
    version = cursor.fetchone()
    print(f"✅ PostgreSQL 연결 성공!")
    print(f"   버전: {version[0][:80]}...")

    cursor.execute("SELECT * FROM pg_extension WHERE extname = 'vector';")
    vector_ext = cursor.fetchone()
    print(f"✅ pgvector 확장: {vector_ext[5] if vector_ext else 'N/A'}")

    cursor.close()
    conn.close()

except Exception as e:
    print(f"❌ 오류: {e}")
```

#### 3.4 DBeaver/CloudBeaver 연결

**DBeaver (데스크톱 앱) 사용 (권장)**

1. **새 연결 생성**: PostgreSQL 선택
2. **연결 정보 입력**:
   - Host: `localhost`
   - Port: `5432`
   - Database: `ddoksori`
   - Username: `postgres`
   - Password: `.env` 파일의 `POSTGRES_PASSWORD`
3. **연결 테스트** → **완료**

**CloudBeaver (웹) 사용 (선택사항)**

CloudBeaver를 사용하려면 `docker-compose.yml`에서 cloudbeaver 서비스 주석을 해제하세요.

1. **docker-compose.yml 수정**: cloudbeaver 서비스 주석 해제
2. **컨테이너 재시작**: `docker-compose up -d`
3. **브라우저 접속**: http://localhost:8978
4. **서버 추가**:
   - Host: `postgres` (Docker 네트워크 내부 호스트명)
   - Port: `5432`
   - Database: `ddoksori`
   - Username: `postgres`
   - Password: `.env` 파일의 `POSTGRES_PASSWORD`

---

### 4단계: 스키마 확인

Docker Compose가 자동으로 `01_00_unified_schema.sql`을 실행했는지 확인합니다.

```bash
# PostgreSQL 컨테이너 접속
docker exec -it ddoksori_db psql -U postgres -d ddoksori

# 테이블 확인
\dt

# 인덱스 확인
\di

# 함수 확인
\df search_*
```

**예상 출력**:
```
                  List of relations
 Schema |         Name          | Type  |  Owner
--------+-----------------------+-------+----------
 public | search_quality_logs   | table | postgres
 public | vector_chunks         | table | postgres
```

---

### 5단계: Docker 명령어 모음

#### 일상적인 명령어

```bash
# 컨테이너 시작
docker-compose up -d

# 컨테이너 중지
docker-compose stop

# 컨테이너 중지 및 삭제 (볼륨은 유지)
docker-compose down

# 컨테이너 및 볼륨 모두 삭제 (데이터 삭제 주의!)
docker-compose down -v

# 로그 확인
docker-compose logs -f postgres

# 최근 100줄 로그
docker-compose logs --tail=100 postgres

# 컨테이너 재시작
docker-compose restart postgres
```

#### 데이터 관리

```bash
# 백업 (컨테이너 실행 중)
docker exec ddoksori_db pg_dump -U postgres ddoksori > backup_$(date +%Y%m%d).sql

# 복원 (컨테이너 실행 중)
docker exec -i ddoksori_db psql -U postgres -d ddoksori < backup_20260123.sql

# 볼륨 목록 확인
docker volume ls

# 볼륨 상세 정보
docker volume inspect ddoksori_postgres_data
```

---

### 🚨 Docker 크로스 플랫폼 주의사항

#### 1. 파일 경로 차이

**Windows (docker-compose.yml)**:
```yaml
volumes:
  - ./01_00_unified_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
```

**Mac/Linux (docker-compose.yml)**:
```yaml
# 동일하게 사용 가능 (상대 경로 권장)
volumes:
  - ./01_00_unified_schema.sql:/docker-entrypoint-initdb.d/01_schema.sql:ro
```

**권장**: 상대 경로(`./ 시작`)를 사용하면 모든 OS에서 동일하게 작동합니다.

#### 2. 줄바꿈 문자 (Line Endings)

**문제**: Windows는 CRLF(`\r\n`), Mac/Linux는 LF(`\n`)를 사용합니다.

**해결**:
- Git 설정으로 자동 변환:
  ```bash
  # Windows
  git config --global core.autocrlf true

  # Mac/Linux
  git config --global core.autocrlf input
  ```

- `.gitattributes` 파일 생성:
  ```
  *.sql text eol=lf
  *.sh text eol=lf
  *.yml text eol=lf
  ```

#### 3. Apple Silicon (M1/M2/M3) 주의사항

**pgvector 이미지**: `pgvector/pgvector:pg17`은 **arm64 아키텍처를 지원**합니다.

**확인**:
```bash
# Mac에서 아키텍처 확인
uname -m

# arm64 또는 x86_64 출력
```

**만약 arm64에서 문제 발생 시**:
```yaml
# docker-compose.yml에 platform 명시
services:
  postgres:
    image: pgvector/pgvector:pg17
    platform: linux/amd64  # 또는 linux/arm64
```

#### 4. 볼륨 성능

**Windows (WSL 2)**:
- ✅ **권장**: WSL 2 파일 시스템 내부에 프로젝트 배치
  - 예: `/home/사용자명/project/...`
  - 성능: ~10배 빠름
- ❌ **비권장**: Windows 파일 시스템 (`/mnt/c/Users/...`)
  - 성능: 느림 (파일 시스템 변환 오버헤드)

**Mac**:
- ✅ gRPC FUSE 사용 (Docker Desktop 설정)
- ✅ VirtioFS 사용 (Docker Desktop 4.6+, 성능 개선)

#### 5. 포트 충돌

**Windows/Mac 공통**:
- 기본 포트 5432가 이미 사용 중이면 `.env`에서 변경:
  ```env
  POSTGRES_PORT=5433
  ```

**확인**:
```bash
# Windows
netstat -ano | findstr :5432

# Mac/Linux
lsof -i :5432
```

#### 6. Docker Compose 버전

**Compose V2 사용 권장** (Docker Desktop 기본 제공):
```bash
# V2 (권장)
docker compose up -d

# V1 (레거시, 2023년 지원 종료)
docker-compose up -d
```

**호환성**: `docker-compose`(V1)와 `docker compose`(V2)는 거의 동일하지만, V2가 더 빠르고 안정적입니다.

#### 7. 팀원 간 공유 시 체크리스트

**공유할 파일**:
- ✅ `docker-compose.yml`
- ✅ `01_00_unified_schema.sql`
- ✅ `.env.example` (비밀번호 제외한 템플릿)
- ✅ `README.md` (구축 가이드)

**공유하지 말 것**:
- ❌ `.env` (비밀번호 포함)
- ❌ `data/` (PostgreSQL 데이터 볼륨)

**팀원 온보딩**:
1. Git 저장소 클론
2. `.env.example`을 `.env`로 복사하고 비밀번호 설정
3. `docker-compose up -d` 실행
4. 완료!

---

## 💻 방법 B: 로컬 설치 (Windows)

Docker를 사용할 수 없는 경우에만 이 방법을 사용하세요.

### 사전 요구사항
- Windows 10/11 (64-bit)
- 최소 8GB RAM (16GB 권장)
- 10GB 이상 여유 디스크 공간
- 관리자 권한

---

### 1단계: PostgreSQL 17.7 설치

#### 1.1 설치 파일 다운로드

1. **PostgreSQL 공식 사이트 접속**:
   - URL: https://www.enterprisedb.com/downloads/postgres-postgresql-downloads

2. **버전 선택**:
   - Version: **17.7**
   - Platform: **Windows x86-64**
   - 다운로드 버튼 클릭 (`postgresql-17.7-1-windows-x64.exe`)

#### 1.2 설치 실행

1. **설치 파일 실행** (관리자 권한):
   ```
   postgresql-17.7-1-windows-x64.exe
   ```

2. **설치 경로 선택**:
   - 기본값 권장: `C:\Program Files\PostgreSQL\17`
   - **중요**: 설치 경로를 메모해두세요!

3. **구성 요소 선택**:
   - [✓] PostgreSQL Server (필수)
   - [ ] pgAdmin 4 (DBeaver 사용 예정이므로 불필요)
   - [✓] Command Line Tools (필수)
   - [ ] Stack Builder (불필요)

4. **데이터 디렉토리 선택**:
   - 기본값 권장: `C:\Program Files\PostgreSQL\17\data`

5. **슈퍼유저 비밀번호 설정**:
   - **매우 중요**: 강력한 비밀번호 설정
   - 예: `YourSecurePassword123!`
   - **반드시 메모해두세요!** (나중에 연결 시 필요)

6. **포트 번호 설정**:
   - 기본값: **5432**
   - 다른 프로그램과 충돌 시 5433 등으로 변경

7. **로케일 설정**:
   - 기본값 사용 (`Korean, Korea`)

8. **설치 완료**:
   - "Finish" 클릭
   - Stack Builder 체크 해제 (나중에 필요 시 실행)

#### 1.3 설치 확인

1. **시작 메뉴에서 "SQL Shell (psql)" 실행**:
   ```
   Server [localhost]:          (Enter - 기본값 사용)
   Database [postgres]:         (Enter - 기본값 사용)
   Port [5432]:                 (Enter - 기본값 사용)
   Username [postgres]:         (Enter - 기본값 사용)
   Password for user postgres:  (설치 시 설정한 비밀번호 입력)
   ```

2. **버전 확인**:
   ```sql
   SELECT version();
   ```

   **예상 출력**:
   ```
   PostgreSQL 17.7 on x86_64-pc-windows-msvc, compiled by Visual C++ ...
   ```

3. **연결 성공 확인**:
   ```sql
   \conninfo
   ```

   **예상 출력**:
   ```
   You are connected to database "postgres" as user "postgres" via socket in "localhost" at port "5432".
   ```

#### 1.4 환경 변수 설정 (선택사항, 권장)

1. **시스템 환경 변수 편집**:
   - "내 PC" 우클릭 → "속성" → "고급 시스템 설정" → "환경 변수"

2. **시스템 변수 → Path 편집**:
   - 새로 만들기 클릭
   - 추가: `C:\Program Files\PostgreSQL\17\bin`
   - 확인 클릭

3. **PowerShell/CMD에서 확인**:
   ```bash
   psql --version
   ```

   **예상 출력**:
   ```
   psql (PostgreSQL) 17.7
   ```

---

### 2단계: pgvector 0.8.1 설치

#### 2.1 설치 방법 선택

**Windows에서 pgvector 설치는 두 가지 방법이 있습니다:**

**방법 A: 사전 컴파일된 바이너리 사용 (권장, 초보자용)**

1. **pgvector Windows 바이너리 다운로드**:
   - URL: https://github.com/pgvector/pgvector/releases/tag/v0.8.1
   - 파일: `pgvector-v0.8.1-windows-x64-pg17.zip` 다운로드

2. **압축 해제 및 파일 복사**:
   ```powershell
   # PowerShell을 관리자 권한으로 실행

   # 다운로드한 zip 파일 압축 해제 (다운로드 폴더에서)
   Expand-Archive -Path "$env:USERPROFILE\Downloads\pgvector-v0.8.1-windows-x64-pg17.zip" -DestinationPath "C:\temp\pgvector"

   # DLL 파일 복사
   Copy-Item "C:\temp\pgvector\vector.dll" -Destination "C:\Program Files\PostgreSQL\17\lib"

   # SQL 파일 복사
   Copy-Item "C:\temp\pgvector\vector--0.8.1.sql" -Destination "C:\Program Files\PostgreSQL\17\share\extension"
   Copy-Item "C:\temp\pgvector\vector.control" -Destination "C:\Program Files\PostgreSQL\17\share\extension"
   ```

**방법 B: 소스에서 컴파일 (고급 사용자용)**

만약 사전 컴파일된 바이너리가 작동하지 않으면:

1. **Visual Studio 2022 설치** (Community Edition):
   - URL: https://visualstudio.microsoft.com/downloads/
   - "Desktop development with C++" 워크로드 선택

2. **pgvector 소스 다운로드 및 컴파일**:
   ```bash
   git clone --branch v0.8.1 https://github.com/pgvector/pgvector.git
   cd pgvector
   nmake /F Makefile.win
   nmake /F Makefile.win install
   ```

#### 2.2 설치 확인

1. **psql 접속**:
   ```bash
   psql -U postgres -d postgres
   ```

2. **pgvector 확장 생성 테스트**:
   ```sql
   CREATE EXTENSION vector;
   ```

   **성공 시 출력**:
   ```
   CREATE EXTENSION
   ```

3. **설치된 버전 확인**:
   ```sql
   SELECT * FROM pg_extension WHERE extname = 'vector';
   ```

   **예상 출력**:
   ```
    oid  | extname | extowner | extnamespace | extrelocatable | extversion | ...
   ------+---------+----------+--------------+----------------+------------+-----
    xxxxx| vector  |       10 |         2200 | t              | 0.8.1      | ...
   ```

4. **벡터 타입 테스트**:
   ```sql
   SELECT '[1,2,3]'::vector(3);
   ```

   **예상 출력**:
   ```
    vector
   ---------
    [1,2,3]
   ```

5. **확장 제거 (다음 단계를 위해)**:
   ```sql
   DROP EXTENSION vector;
   ```

---

## 🗄️ 데이터베이스 세팅

이 섹션은 Docker 방식과 로컬 설치 방식 **모두에 공통**으로 적용됩니다.

### 1단계: 데이터베이스 생성

#### Docker 사용 시

```bash
# PostgreSQL 컨테이너 접속
docker exec -it ddoksori_db psql -U postgres
```

#### 로컬 설치 시

```bash
# psql 접속
psql -U postgres
```

#### 데이터베이스 생성 (공통)

```sql
-- 이미 존재하는지 확인
\l ddoksori

-- 없으면 생성
CREATE DATABASE ddoksori
    WITH
    OWNER = postgres
    ENCODING = 'UTF8'
    LC_COLLATE = 'C'
    LC_CTYPE = 'C'
    TEMPLATE = template0;

-- 연결
\c ddoksori
```

**성공 시 출력**:
```
You are now connected to database "ddoksori" as user "postgres".
```

---

### 2단계: pgvector 확장 설치

```sql
CREATE EXTENSION IF NOT EXISTS vector;
```

**확인**:
```sql
\dx vector
```

**예상 출력**:
```
                      List of installed extensions
  Name   | Version | Schema |                Description
---------+---------+--------+------------------------------------------
 vector  | 0.8.1   | public | vector data type and ivfflat and hnsw access methods
```

---

### 3단계: 스키마 실행

#### 스키마 파일 위치

```
data_n_db/DB/01_00_unified_schema.sql
```

#### 실행 방법

**방법 A: psql 명령어 (권장)**

```bash
# PowerShell/CMD (프로젝트 루트에서)
cd data_n_db/DB

# Docker 사용 시
docker exec -i ddoksori_db psql -U postgres -d ddoksori < 01_00_unified_schema.sql

# 로컬 설치 시
psql -U postgres -d ddoksori -f 01_00_unified_schema.sql
```

**방법 B: psql 내부에서 실행**

```sql
-- psql 접속 후
\c ddoksori
\i 'C:/Users/Playdata/Desktop/project/05_5th_project/data_n_db/DB/01_00_unified_schema.sql'
```

**방법 C: DBeaver GUI**

1. DBeaver 실행 및 ddoksori 데이터베이스 연결
2. 연결 우클릭 → SQL Editor → New SQL Script
3. File → Open → `01_00_unified_schema.sql` 선택
4. Ctrl+Enter 또는 "Execute SQL Script" 버튼

---

### 4단계: 스키마 생성 확인

```sql
-- 테이블 확인
\dt
```

**예상 출력**:
```
                  List of relations
 Schema |         Name          | Type  |  Owner
--------+-----------------------+-------+----------
 public | search_quality_logs   | table | postgres
 public | vector_chunks         | table | postgres
```

```sql
-- 인덱스 확인 (16개 이상)
\di
```

**예상 인덱스**:
- `idx_chunk_id`
- `idx_dataset_type`
- `idx_category`
- `idx_law_name`
- `idx_chunk_type`
- `idx_embedding_hnsw` (HNSW 벡터 인덱스)
- `idx_text_tsv` (GIN 전문 검색 인덱스)
- `idx_metadata_gin` (GIN JSONB 인덱스)
- 기타 복합 인덱스들...

```sql
-- 함수 확인
\df search_*
```

**예상 함수**:
- `search_bm25`
- `search_hybrid_rrf`
- `search_hybrid`

```sql
-- 테이블 구조 확인
\d vector_chunks
```

**16개 컬럼 확인**:
- `id`, `chunk_id`, `dataset_type`, `text`, `embedding`
- `law_name`, `chunk_type`, `category`
- `source_url`, `source_file`, `printed_page`, `source_year`
- `metadata`, `text_tsv`
- `created_at`, `updated_at`

---

### 5단계: 샘플 데이터 테스트

```sql
-- 테스트 벡터 생성 (1536차원 zero vector)
DO $$
DECLARE
    test_vector vector(1536);
BEGIN
    -- 0으로 채워진 1536차원 벡터 생성
    test_vector := array_fill(0.0, ARRAY[1536])::vector(1536);

    -- 테스트 데이터 삽입
    INSERT INTO vector_chunks
    (chunk_id, dataset_type, text, embedding, category)
    VALUES
    ('test_001', 'case', '테스트 데이터입니다.', test_vector, '상담');

    RAISE NOTICE '✅ 테스트 데이터 삽입 성공!';
END $$;

-- 확인
SELECT chunk_id, dataset_type, text, category
FROM vector_chunks
WHERE chunk_id = 'test_001';

-- 벡터 검색 테스트
SELECT chunk_id, 1 - (embedding <=> array_fill(0.0, ARRAY[1536])::vector(1536)) AS similarity
FROM vector_chunks
WHERE chunk_id = 'test_001';

-- 테스트 데이터 삭제
DELETE FROM vector_chunks WHERE chunk_id = 'test_001';
```

---

## 📥 데이터 삽입

데이터 삽입은 별도의 Python 스크립트로 진행합니다.

### 1. Python 환경 구성

#### 가상 환경 생성

```bash
# 프로젝트 루트로 이동
cd data_n_db

# 가상 환경 생성
python -m venv venv

# 활성화 (PowerShell)
.\venv\Scripts\Activate.ps1

# 활성화 (CMD)
.\venv\Scripts\activate.bat

# 활성화 (Mac/Linux)
source venv/bin/activate
```

#### 필수 패키지 설치

```bash
# pip 업그레이드
python -m pip install --upgrade pip

# PostgreSQL 드라이버
pip install psycopg2-binary

# 벡터 연산
pip install numpy

# 임베딩 API
pip install openai

# 데이터 처리
pip install python-dotenv
```

### 2. 환경 변수 설정

`.env` 파일이 이미 있으면 확인만 하세요.

```env
# PostgreSQL 연결 정보
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=ddoksori
POSTGRES_USER=postgres
POSTGRES_PASSWORD=YourSecurePassword123!

# OpenAI API
OPENAI_API_KEY=sk-proj-...
```

### 3. 연결 테스트

```python
# test_connection.py
import os
import psycopg2
from dotenv import load_dotenv

load_dotenv()

try:
    conn = psycopg2.connect(
        host=os.getenv("POSTGRES_HOST"),
        port=os.getenv("POSTGRES_PORT"),
        database=os.getenv("POSTGRES_DB"),
        user=os.getenv("POSTGRES_USER"),
        password=os.getenv("POSTGRES_PASSWORD")
    )

    cursor = conn.cursor()

    # PostgreSQL 버전
    cursor.execute("SELECT version();")
    version = cursor.fetchone()
    print(f"✅ PostgreSQL 연결 성공!")
    print(f"   버전: {version[0][:80]}...")

    # pgvector 확장
    cursor.execute("SELECT * FROM pg_extension WHERE extname = 'vector';")
    vector_ext = cursor.fetchone()
    print(f"✅ pgvector 확장: {vector_ext[5] if vector_ext else 'N/A'}")

    # 테이블 확인
    cursor.execute("SELECT COUNT(*) FROM information_schema.tables WHERE table_name = 'vector_chunks';")
    table_exists = cursor.fetchone()[0]
    print(f"✅ vector_chunks 테이블: {'존재함' if table_exists else '없음'}")

    cursor.close()
    conn.close()

    print("\n✨ 환경 준비 완료! 데이터 삽입을 시작할 수 있습니다.")

except Exception as e:
    print(f"❌ 오류 발생: {e}")
```

**실행**:
```bash
python test_connection.py
```

---

## 🔧 문제 해결

### Docker 관련 문제

#### 문제 1: Docker 데몬이 실행되지 않음

**증상**:
```
Cannot connect to the Docker daemon at unix:///var/run/docker.sock
```

**해결**:
- Windows: Docker Desktop을 시작 메뉴에서 실행
- Mac: Applications에서 Docker 실행
- 시스템 트레이에 Docker 아이콘이 나타날 때까지 대기

#### 문제 2: 포트가 이미 사용 중

**증상**:
```
Bind for 0.0.0.0:5432 failed: port is already allocated
```

**해결**:
```bash
# 포트 사용 확인
# Windows
netstat -ano | findstr :5432

# Mac
lsof -i :5432

# .env 파일에서 포트 변경
POSTGRES_PORT=5433
```

#### 문제 3: 볼륨 권한 문제 (Mac/Linux)

**증상**:
```
Permission denied: /var/lib/postgresql/data
```

**해결**:
```bash
# 볼륨 삭제 후 재생성
docker-compose down -v
docker-compose up -d
```

#### 문제 4: 이미지 pull 실패

**증상**:
```
Error response from daemon: manifest for pgvector/pgvector:pg17 not found
```

**해결**:
```bash
# 이미지 수동 pull
docker pull pgvector/pgvector:pg17

# 또는 latest 태그 사용
docker pull pgvector/pgvector:latest
```

---

### PostgreSQL 관련 문제

#### 문제 1: 비밀번호 인증 실패

**증상**:
```
FATAL: password authentication failed for user "postgres"
```

**해결**:
```bash
# Docker 사용 시 - 비밀번호 재설정
docker exec -it ddoksori_db psql -U postgres
# 또는 컨테이너 재생성
docker-compose down -v
docker-compose up -d

# 로컬 설치 시 - pg_hba.conf 확인
# C:\Program Files\PostgreSQL\17\data\pg_hba.conf
# 다음 라인 확인:
# host    all             all             127.0.0.1/32            scram-sha-256
```

#### 문제 2: 데이터베이스 연결 거부

**증상**:
```
could not connect to server: Connection refused
```

**해결**:
```bash
# Docker 컨테이너 상태 확인
docker-compose ps

# 로그 확인
docker-compose logs postgres

# 로컬 설치 시 - PostgreSQL 서비스 확인
# Windows
Get-Service postgresql-x64-17
Start-Service postgresql-x64-17
```

#### 문제 3: pgvector 확장 오류

**증상**:
```sql
ERROR: could not open extension control file
```

**해결**:
```bash
# Docker 사용 시 - 올바른 이미지 사용 확인
docker images | grep pgvector

# 로컬 설치 시 - 파일 존재 확인
Test-Path "C:\Program Files\PostgreSQL\17\share\extension\vector.control"
Test-Path "C:\Program Files\PostgreSQL\17\lib\vector.dll"
```

#### 문제 4: DBeaver 연결 안 됨

**증상**:
```
Connection refused
또는
Timeout
```

**해결**:
```bash
# Docker 컨테이너 실행 확인
docker-compose ps

# 컨테이너가 실행 중인지 확인
# STATUS가 "Up"이어야 함
```

**DBeaver 연결 정보 재확인**:
- Host: `localhost` (또는 `127.0.0.1`)
- Port: `5432`
- Database: `ddoksori`
- Username: `postgres`
- Password: `.env` 파일의 `POSTGRES_PASSWORD`

**방화벽 확인 (Windows)**:
```powershell
# 포트 5432가 열려 있는지 확인
netstat -ano | findstr :5432
```

---

### Python 연결 문제

#### 문제 1: psycopg2 설치 실패 (Windows)

**증상**:
```
ERROR: Could not build wheels for psycopg2
```

**해결**:
```bash
# psycopg2-binary 사용
pip uninstall psycopg2
pip install psycopg2-binary
```

#### 문제 2: 환경 변수 로드 안 됨

**증상**:
```python
TypeError: 'NoneType' object is not iterable
```

**해결**:
```python
# .env 파일 경로 확인
from dotenv import load_dotenv, find_dotenv

# .env 파일 찾기
env_path = find_dotenv()
print(f"📂 .env 파일 위치: {env_path}")

# 로드
load_dotenv(env_path)
```

---

## ✅ 마이그레이션 체크리스트

### Phase 1: 환경 준비

#### Docker 방식 (권장)
- [ ] Docker Desktop 설치 (Windows/Mac)
- [ ] WSL 2 활성화 (Windows만 해당)
- [ ] `docker-compose.yml` 파일 생성
- [ ] `.env` 파일 생성 (비밀번호 설정)
- [ ] `docker-compose up -d` 실행
- [ ] 컨테이너 상태 확인 (`docker-compose ps`)
- [ ] PostgreSQL 연결 테스트
- [ ] DBeaver 연결 테스트

#### 로컬 설치 방식
- [ ] PostgreSQL 17.7 설치
- [ ] pgvector 0.8.1 확장 설치
- [ ] 환경 변수 설정 (PATH 추가)
- [ ] PostgreSQL 서비스 시작
- [ ] psql 연결 테스트

### Phase 2: 데이터베이스 세팅

- [ ] `ddoksori` 데이터베이스 생성
- [ ] pgvector 확장 설치 (`CREATE EXTENSION vector`)
- [ ] 스키마 실행 (`01_00_unified_schema.sql`)
- [ ] 테이블 생성 확인 (2개 테이블)
- [ ] 인덱스 생성 확인 (16개 이상)
- [ ] 함수 생성 확인 (3개 함수)
- [ ] 샘플 데이터 삽입 테스트

### Phase 3: Python 환경 구성

- [ ] Python 3.10+ 설치
- [ ] 가상 환경 생성 및 활성화
- [ ] 필수 패키지 설치 (`psycopg2-binary`, `openai`, 등)
- [ ] `.env` 파일 설정
- [ ] Python 연결 테스트 (`test_connection.py`)

### Phase 4: 데이터 삽입

- [ ] A_law_ED_guide 삽입 스크립트 작성
- [ ] A_law_ED_guide 6,138개 삽입
- [ ] 데이터 검증 (count, sample 확인)
- [ ] B_case 삽입 스크립트 작성
- [ ] B_case 46,314개 삽입 (semantic chunking 적용)
- [ ] 전체 데이터 검증 (52,452개 확인)

### Phase 5: 인덱스 최적화

- [ ] VACUUM ANALYZE 실행
- [ ] 인덱스 통계 업데이트
- [ ] tsvector 자동 생성 확인 (트리거 동작 검증)

### Phase 6: 테스트

- [ ] BM25 키워드 검색 테스트
- [ ] 순수 벡터 검색 테스트
- [ ] 하이브리드 검색 (RRF) 테스트
- [ ] 필터링 검색 테스트 (연도, 카테고리, 법령명)
- [ ] 성능 벤치마크
  - [ ] BM25: ~3-5ms
  - [ ] 벡터: ~5ms
  - [ ] 하이브리드: ~10-15ms

### Phase 7: 팀원 공유 (Docker 방식)

- [ ] `.env.example` 파일 생성 (비밀번호 제외)
- [ ] `README.md` 작성 (구축 가이드)
- [ ] Git 커밋 및 푸시
  - [ ] `docker-compose.yml`
  - [ ] `01_00_unified_schema.sql`
  - [ ] `.env.example`
  - [ ] `.gitignore` (`.env`, `data/` 제외)
- [ ] 팀원 온보딩 테스트

---

## 📌 다음 단계

환경 준비가 완료되면:

1. **데이터 삽입 스크립트 작성**:
   - `DB/02_02_insert_law_guide.py` (A_law_ED_guide)
   - `DB/02_03_insert_case.py` (B_case)

2. **데이터 삽입 실행**:
   - 예상 시간: 25-30분
   - 총 52,452개 청크 (semantic chunking 적용)

3. **검색 API 구축**:
   - `DB/03_01_search_api.py` (FastAPI)
   - 하이브리드 검색 엔드포인트

4. **RAG 파이프라인 통합**:
   - LLM 통합 (OpenAI GPT-4)
   - 출처 표기 자동화

---

**문서 버전**: 1.0
**최종 수정**: 2026-01-23
**작성자**: 프로젝트 팀
