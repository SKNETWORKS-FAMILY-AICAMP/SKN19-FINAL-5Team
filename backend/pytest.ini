[pytest]
testpaths = scripts/testing
python_files = test_*.py
python_classes = Test*
python_functions = test_*
norecursedirs = app utils .git .venv __pycache__ _archive
addopts = --ignore=scripts/testing/__pycache__
          --ignore=scripts/testing/_archive
          --cov=app
          --cov-report=term-missing
          --cov-fail-under=0
asyncio_mode = strict

# Phase 1.7: 커스텀 마커 등록 (리팩토링)
markers =
    # 기본 마커
    unit: Unit tests - DB 의존성 없음
    integration: Integration tests - PostgreSQL 필요

    # 기능별 마커
    api: API 엔드포인트 테스트
    supervisor: 슈퍼바이저 테스트 (MAS Supervisor)
    agent: 에이전트 테스트
    retrieval: 검색 테스트
    generation: 답변 생성 테스트
    review: 검토 테스트

    # 실행 조건 마커
    slow: 느린 테스트 (LLM 호출 등)
    docker: Docker 환경 필요 (RUN_DOCKER_TESTS=1)
    skip_ci: CI에서 스킵 (GITHUB_ACTIONS 환경)
    llm: LLM API 호출 필요 (OPENAI_API_KEY)
    e2e: E2E 통합 테스트 - 전체 워크플로우 검증 (Docker DB 필요)

    # 데이터 마커
    needs_db: DB 연결 필요
    needs_data: 시드 데이터 필요

    # 비동기 마커
    asyncio: 비동기 테스트 (pytest-asyncio)
